import React, { FunctionComponent } from "react";
import { View, Text } from "react-native";

const Suggestions : FunctionComponent = () : React.JSX.Element => {
    return (
        <View>
            <Text>Suggestions</Text>
        </View>
    );
}

export default Suggestions;