import React, { FunctionComponent } from "react";
import { View, Text } from "react-native";

const Favourites : FunctionComponent = () : React.JSX.Element => {
    return (
        <View>
            <Text>Favourites</Text>
        </View>
    );
}

export default Favourites;