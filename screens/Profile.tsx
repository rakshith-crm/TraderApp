import React, { FunctionComponent } from "react";
import { View, Text } from "react-native";

const Profile : FunctionComponent = () : React.JSX.Element => {
    return (
        <View>
            <Text>Profile</Text>
        </View>
    );
}

export default Profile;